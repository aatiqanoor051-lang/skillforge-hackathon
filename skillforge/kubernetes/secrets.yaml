# Template only — DO NOT commit real secrets to version control.
# Populate real values via `kubectl create secret` or a secrets manager
# (e.g. Sealed Secrets, External Secrets Operator, AWS Secrets Manager)
# and never apply this file as-is in production.
apiVersion: v1
kind: Secret
metadata:
  name: skillforge-secrets
  namespace: skillforge
type: Opaque
stringData:
  MONGO_INITDB_ROOT_USERNAME: "skillforge_admin"
  MONGO_INITDB_ROOT_PASSWORD: "REPLACE_ME"
  MONGO_URI: "mongodb://skillforge_admin:REPLACE_ME@mongo:27017/skillforge?authSource=admin"
  JWT_SECRET: "REPLACE_ME_WITH_A_LONG_RANDOM_STRING"
  AI_API_KEY: ""
